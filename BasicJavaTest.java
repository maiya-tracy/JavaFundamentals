public class BasicJavaTest {
public static void main(String[] args) {
        BasicJava javs = new BasicJava();
        // javs.oneTo255();
        // javs.oddOneTo255();
        // javs.printSumOneTo255();

        int[] X = {1,3,5,7,9,13};
        int[] Y = {-3, -5, -7};
        int[] Z = {2, 10, -3};
        int num = 4;

        // javs.printArray(X);
        // javs.findMax(X);
        // javs.findMax(Y);
        // javs.getAverage(X);
        // javs.getAverage(Y);
        // javs.getAverage(Z);

        // javs.createOddArray();
        // javs.greaterThanY(X, num);
        // javs.greaterThanY(Y, num);
        // javs.greaterThanY(Z, num);
        // javs.squareValues(X);
        // javs.squareValues(Y);
        // javs.squareValues(Z);
        // javs.eliminateNegatives(X);
        // javs.eliminateNegatives(Y);
        // javs.eliminateNegatives(Z);

        // double[] maxminavg = javs.maxMinAvg(X);
        // System.out.println(maxminavg[0]);
        // System.out.println(maxminavg[1]);
        // System.out.println(maxminavg[2]);

        // javs.shiftValues(X);
        // javs.shiftValues(Y);
        // javs.shiftValues(Z);
}
}
